import 'package:flutter/material.dart';

class BuyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buy Screen'),
      ),
      body: Center(
        child: Text('Buy Screen Content'),
      ),
    );
  }
}